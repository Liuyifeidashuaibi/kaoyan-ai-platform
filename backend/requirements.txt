# FastAPI 后端依赖 — Python 3.11+
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
python-dotenv>=1.0.1
pydantic>=2.9.0
pydantic-settings>=2.6.0

# 数据库
sqlalchemy>=2.0.36
aiosqlite>=0.20.0
psycopg[binary]>=3.2.0    # PostgreSQL 驱动（商用正式环境）

# 阿里云百炼 / DashScope
dashscope>=1.20.0
openai>=1.55.0

# RAG — LlamaIndex + Chroma
llama-index>=0.12.0
llama-index-vector-stores-chroma>=0.4.0
llama-index-embeddings-dashscope>=0.3.0
llama-index-llms-openai-like>=0.3.0
chromadb>=0.5.0

# Agent — LangGraph
langgraph>=0.2.60
langgraph-checkpoint>=2.0.0
langchain-core>=0.3.0

# 文档解析
pypdf>=5.1.0
aiofiles>=24.1.0

# Translator 引擎依赖（已内嵌到 backend/translator/）
pyyaml>=6.0
pymupdf>=1.24          # PDF / 图文文档解析
faster-whisper>=1.0.0  # 视频字幕语音识别
soundfile>=0.12        # 音频读写

# Redis 缓存 + Celery 异步任务（本地单机）
redis>=5.0.0
celery[redis]>=5.4.0

# 本地 TTS — Piper CPU 兜底
piper-tts>=1.2.0
numpy>=1.26.0

# Supabase 向量同步 / 图片压缩 / 社区模块
supabase>=2.5.0
PyJWT>=2.8.0
httpx>=0.27.0
Pillow>=10.4.0

# Translator export email attachments
python-docx>=1.1.2
reportlab>=4.2.0
email-validator>=2.2.0

# Agent 商业级工具依赖
openpyxl>=3.1.0          # Excel 生成
python-pptx>=1.0.0      # PowerPoint 生成
markdown>=3.5.0         # Markdown 渲染增强
chardet>=5.0.0          # 文件编码自动检测
litellm>=1.50.0         # 统一多模型接入层（GPT-4o/通义千问/GLM）
minio>=7.2.0            # MinIO 对象存储
unstructured>=0.16.0    # 多格式文档解析（Word/PDF/表格/OCR）
pandas>=2.2.0           # 数据清洗与分析
